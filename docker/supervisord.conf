[supervisord]
nodaemon=true
logfile=/app/tmp/supervisord.log
pidfile=/app/tmp/supervisord.log

[program:sidekiq]
environment=RAILS_ENV=production
command=bundle exec sidekiq
stdout_logfile=/app/log/production.log

[program:puma]
command=/bin/bash docker/infinite_loop.sh
